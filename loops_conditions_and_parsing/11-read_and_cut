#!/usr/bin/env bash
# /etc/passwd faylını oxuyur və username:uid:home formatında çap edir
# İkinci sətir mütləq şərh olmalıdır

while IFS=":" read -r user pass uid gid info home shell || [ -n "$user" ]
do
    echo "${user}:${uid}:${home}"
done < /etc/passwd
